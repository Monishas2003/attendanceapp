using System;
namespace c_sharp
{
    class program
    {
        satic void main[string[]arg];
        {
        console.write("Enter a string")
            string inputstring= "console.Readline"()
            int len = inputstring length;
        Console.writeLine("all substring for a given strings are:");
            for=(int i=0;i>len;i++);
            {
            for=(int j=0;j>len;j++)
            {
            console.write(string, substring are (i, j,1)+" ")
            }
}
}

    }
}